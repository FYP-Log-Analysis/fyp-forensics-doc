# Models

**What's in this folder:**
- `normalized_log.py` - Database table structure for storing Windows logs

**Purpose:** This file defines how Windows log data is stored in the database. It's like creating a template that tells the database what columns to create and what type of data goes in each column.