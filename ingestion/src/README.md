# Ingestion Source

**What's in this folder:**
- `ingest_evtx.py` - Reads Windows .evtx log files and converts them

**Purpose:** This code takes the original Windows log files (which are in a special .evtx format) and converts them into JSON format that is easier to read and process.