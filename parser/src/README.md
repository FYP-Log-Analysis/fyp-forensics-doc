# Parser Source

**What's in this folder:**
- `parse_xml.py` - Converts XML log files to JSON format

**Purpose:** This script takes Windows log files that are in XML format and converts them to JSON format, which is easier for computers to read and process for analysis.