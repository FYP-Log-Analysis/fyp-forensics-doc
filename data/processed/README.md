# Processed

**What's in this folder:**
- `xml/` - Log files converted to XML format
- `json/` - Log files converted to JSON format
- `normalized/` - Cleaned and standardized log files

**Purpose:** This folder contains Windows logs that have been converted from the original .evtx format into easier-to-read formats like XML and JSON. The normalized folder has logs that are cleaned and organized consistently.